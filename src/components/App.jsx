import { StyledWrapper } from './App.styled';
import { ServiceCards } from './ServiceCards/ServiceCards';

export const App = () => {
  return (
    <StyledWrapper>
      <ServiceCards />
    </StyledWrapper>
  );
};
